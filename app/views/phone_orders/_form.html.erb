<%= form_for(phone_order) do |f| %>
  <% if phone_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(phone_order.errors.count, "error") %> prohibited this phone_order from being saved:</h2>

      <ul>
      <% phone_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :phone %>
    <%= f.text_field :phone %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
